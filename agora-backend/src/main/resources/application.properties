spring.application.name=agora-backend

# Disable Docker Compose support
spring.docker.compose.enabled=false

# gRPC Transcription Service Configuration
transcription.grpc.host=localhost
transcription.grpc.port=50051
